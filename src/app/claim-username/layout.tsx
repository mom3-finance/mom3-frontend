export const metadata = {
  title: "Claim Username | Oni",
  description: "Claim your Oni username before entering your dashboard.",
};

export default function ClaimUsernameLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
