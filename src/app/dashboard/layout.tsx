export const metadata = {
  title: "Dashboard | Oni",
  description: "Manage your Oni balance, assets, and rewards.",
};

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
